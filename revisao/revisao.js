function revisao(){
    let nome = "João";
    console.log(nome);
    if (true){
        let sobrenome = "Palandi";
        console.log(sobrenome);
    }
    const idade = 40;
    console.log(idade);

    const precoOriginal = 100;
    const desconto = 20;
    let precoFinal = precoOriginal - desconto;

    console.log("Preço original: " + precoOriginal);
    console.log("Deconto: " + desconto);
    console.log("Preço final: " + precoFinal)
}
revisao();
